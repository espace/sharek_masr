 
 var facebookAndSessionLogout = function(current_user_id){  
         //logout_path =  "http://" + window.location.host+"/logout";
        // url =  "http://" + window.location.host+"/login";
//         FB.logout(function(response) {  });
 };
 
  var facebookLogin =  function(access_token,uid,user_name,email){ 
    return 0;
/*    oldURL = new String(window.location);
    if(locale == 'ar'){
      url =  "http://" + window.location.host+"/ar/user_sessions";
    }
    else{
       url =  "http://" + window.location.host+"/user_sessions";
    }
     
     $.ajax(
    {  
       type: "POST",
       url: url,
       data: {user_session: {email: email,
         password: uid},
         uid: uid,
         access_token: access_token,
         email: email,
         user_name: user_name}  ,
      
      success: function(response){
        if(oldURL.match("/login") == "/login" || oldURL.match("/signup") == "/signup")
        {
           if(locale == 'ar'){
            oldURL = "http://" + window.location.host+"/ar";
           }
            else{
            oldURL = "http://" + window.location.host;
           }
        }
          window.location = oldURL;  
      }
    });   
*/ };
 
 
 




